# Interface pública

A primeira interface está em `web/index.html`.

É um protótipo estático e deliberadamente pequeno. Neste momento contém o caso de teste `512044821` → `FARMÁCIA CORREA` para validar a experiência de pesquisa antes de ligar a interface à API/base de dados.

## Próximo passo

Substituir o objeto `seed` por um pedido à API pública do projeto. A interface não deverá conhecer nem conter os dados empresariais diretamente.
